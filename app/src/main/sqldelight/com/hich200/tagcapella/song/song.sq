selectAll:
SELECT * FROM song;

selectSong:
SELECT * FROM song WHERE path = ?;

insertSong:
INSERT INTO song (path, title) VALUES (?, ?);

deleteSong:
DELETE FROM song WHERE id = ?;

selectSongTags:
SELECT tag.tag FROM song_tag JOIN tag ON song_tag.tag = tag.id WHERE song = ?;